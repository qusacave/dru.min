//------------------------------------------------------------------------------
//  照度センサーの値が変わると呼ばれる関数
//
//  MEMO: e.value の中に現在の明るさが入っている
//------------------------------------------------------------------------------
deviceMotion = function( e ) {
    //changeScreen( e );  //  画面を変化させる処理を呼ぶ
    changeAcceleration( e );   //  音を変化させる処理を呼ぶ
};

//  イベントハンドラを設定
window.addEventListener( 'devicemotion', deviceMotion );
